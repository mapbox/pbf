syntax = "proto3";

message Envelope {
    message KvEntry {
        int32 marker = 1;
    }
    enum KnEntry {
        ZERO = 0;
        ONE = 1;
    }
    map<string, string> kv = 1;
    map<string, int32> kn = 2;
    Envelope.KvEntry sibling = 3;
    Envelope.KnEntry flag = 4;
}
